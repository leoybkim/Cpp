#ifndef UTILS_H_	// This can be anything but normally it follows the file name
#define UTILS_H_	// In my case I had numbers, but I can't use numbers in the beginning
			// only runs if the symbol is NOT defined
void hello2();

#endif /* UTILS_H_ */
